# Proxy list for browser automation
# Format: protocol://[user:pass@]host:port
# Lines starting with # are ignored

# Single proxy (supports rotating residential proxies)
# http://user:pass@proxy.example.com:8080

# Multiple proxies (round-robin rotation)
# http://user1:pass1@proxy1.example.com:8080
# socks5://user2:pass2@proxy2.example.com:1080
# http://proxy3.example.com:3128

# Leave empty or delete file to run without proxy
